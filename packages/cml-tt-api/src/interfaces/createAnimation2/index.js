import Index from './index.interface';
import CreateAnimationFactory from 'chameleon-api/src/interfaces/createAnimation/createAnimationFactory';

console.log(Index)
Index.initAnimation();

// export default (description) => new CreateAnimationFactory(description);
